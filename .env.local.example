# StudyNook client — copy to .env.local
# Auth and all API routes live on studynook-server; the client only proxies them.

# Express API base URL (no trailing slash)
# Local:  http://localhost:5000
# Prod:   https://studynook-server-kappa.vercel.app
NEXT_PUBLIC_API_URL=http://localhost:5000
